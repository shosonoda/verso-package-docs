# Demonstration Package Documentation

This is a demonstration of documentation for a Lean package, built in Verso's manual genre.
The code being documented is in the sibling [`../zippers/`](../zippers/) directory.

To build it, run:
```
$ lake exe docs
```

The generated site will be in `_out/html-multi/`.
